# Shared password gate (client-only). Leave blank to disable the gate.
VITE_APP_PASSWORD=

# Worker host that the SPA connects to. Leave blank for local dev (127.0.0.1:1999).
# In production, set to the wrangler-deployed worker URL, e.g. suspect-game-server.<account>.workers.dev
VITE_PARTYKIT_HOST=
